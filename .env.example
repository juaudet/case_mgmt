# ── Backend ─────────────────────────────────────────────────────────────────
# In Docker Compose, prefer *_FILE paths (see README). For local `uvicorn`, plain vars are fine.
MONGODB_URL=mongodb+srv://user:pass@cluster.mongodb.net/siem
# MONGODB_URL_FILE=/run/secrets/mongodb_url
REDIS_URL=redis://localhost:6379
# REDIS_URL_FILE=/run/secrets/redis_url
JWT_SECRET_KEY=<64-char random hex — openssl rand -hex 32>
# JWT_SECRET_KEY_FILE=/run/secrets/jwt_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
# ANTHROPIC_API_KEY=sk-ant-...   # optional local dev without Docker secrets
ANTHROPIC_API_KEY_FILE=/run/secrets/anthropic_api_key
DEMO_MODE=true                    # true = use mock fixtures, no live API calls
MOCK_INCIDENT_FEED=true           # with DEMO_MODE: background synthetic incidents to MongoDB
# When DEMO_MODE=false, enrichment + /mcp/run call Tines directly (no sidecar required).
# Prefer TINES_WEBHOOK_URL; TINES_MCP_URL is accepted as an alias.
TINES_WEBHOOK_URL=https://<YOUR-TINES-TENANT>.tines.com/mcp/<YOUR-TOKEN>
# TINES_MCP_URL=https://...   # legacy alias, same purpose as TINES_WEBHOOK_URL
# Optional: override Tines story names if yours differ (tools/list on the tenant).
# TINES_VT_TOOL=search_for_files_urls_domains_ips_and_comments
# TINES_ABUSEIPDB_CHECK_TOOL=search_for_an_ip_address
# TINES_ABUSEIPDB_REPORTS_TOOL=get_reports_for_an_ip_address

# ── Frontend (Next.js) ───────────────────────────────────────────────────────
# NEXTAUTH_SECRET=...   # local dev; Docker image reads NEXTAUTH_SECRET_FILE via entrypoint
NEXTAUTH_SECRET_FILE=/run/secrets/nextauth_secret
NEXTAUTH_URL=https://your-domain.com
NEXT_PUBLIC_API_URL=https://api.your-domain.com
